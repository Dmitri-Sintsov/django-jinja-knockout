============
Installation
============

At the command line::

    $ easy_install django-jinja-knockout

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-jinja-knockout
    $ pip install django-jinja-knockout
